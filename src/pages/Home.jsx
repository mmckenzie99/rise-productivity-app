import { useMemo,useState } from 'react';import { useAuth } from '@/lib/AuthContext';import AppHeader from '@/components/speaking/AppHeader';import Dashboard from '@/components/speaking/Dashboard';import EngagementMap from '@/components/speaking/EngagementMap';import Filters from '@/components/speaking/Filters';import EngagementCard from '@/components/speaking/EngagementCard';import EngagementForm from '@/components/speaking/EngagementForm';import EngagementDetail from '@/components/speaking/EngagementDetail';import InviteDialog from '@/components/speaking/InviteDialog';import TimelineDialog from '@/components/speaking/TimelineDialog';import useEngagements from '@/hooks/useEngagements';
export default function Home(){const {user}=useAuth();const isAdmin=user?.role==='admin';const {items,loading,save,remove}=useEngagements(),[filters,setFilters]=useState({status:'all',progress:'all',search:''}),[form,setForm]=useState(false),[selected,setSelected]=useState(null),[invite,setInvite]=useState(false),[timeline,setTimeline]=useState(false);const visible=useMemo(()=>items.filter(x=>(filters.status==='all'||x.status===filters.status)&&(filters.progress==='all'||x.progress===filters.progress)&&`${x.title} ${x.speaker_name} ${x.address}`.toLowerCase().includes(filters.search.toLowerCase())),[items,filters]);const edit=x=>{setSelected(null);setForm(x)};const del=async x=>{if(window.confirm(`Delete “${x.title}”?`)){await remove(x.id);setSelected(null)}};return <main className="min-h-screen bg-[#E7DFC6] text-[#1F2D3B]"><div className="mx-auto max-w-6xl space-y-7 px-4 py-6 sm:px-6 sm:py-9"><AppHeader onAdd={()=>setForm(true)} onInvite={()=>setInvite(true)} onTimeline={()=>setTimeline(true)} isAdmin={isAdmin}/><Dashboard items={items}/><EngagementMap items={visible} onView={setSelected}/><Filters filters={filters} setFilters={setFilters}/>{loading?<div className="py-14 text-center">Loading engagements…</div>:visible.length?<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{visible.map(x=><EngagementCard key={x.id} item={x} onClick={setSelected}/>)}</div>:<div className="rounded-lg border border-dashed border-[#A9793B] bg-[#F6F1E4]/60 py-14 text-center"><h2 className="font-display text-xl font-semibold">{items.length?'Nothing matches':'No engagements yet'}</h2><p className="mt-2 text-sm text-[#4C5B6B]">{items.length?'Try a different filter.':isAdmin?'Add your first speaking engagement to see it mapped here.':'Ask an administrator to add one.'}</p></div>}</div><EngagementForm open={!!form} item={form===true?null:form} onClose={()=>setForm(false)} onSave={save}/><EngagementDetail item={selected} onClose={()=>setSelected(null)} onEdit={edit} onDelete={del} isAdmin={isAdmin}/><InviteDialog open={invite} onClose={()=>setInvite(false)}/><TimelineDialog open={timeline} onClose={()=>setTimeline(false)} items={items}/></main>}