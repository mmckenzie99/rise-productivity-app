import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';
import { PROGRESS,STATUSES } from '@/lib/speaking';
export default function Filters({filters,setFilters}){return <div className="flex flex-col gap-3 sm:flex-row"><Select value={filters.status} onValueChange={status=>setFilters({...filters,status})}><SelectTrigger className="bg-white sm:w-44"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">All statuses</SelectItem>{STATUSES.map(x=><SelectItem key={x} value={x}>{x}</SelectItem>)}</SelectContent></Select><Select value={filters.progress} onValueChange={progress=>setFilters({...filters,progress})}><SelectTrigger className="bg-white sm:w-48"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">All progress</SelectItem>{PROGRESS.map(x=><SelectItem key={x} value={x}>{x}</SelectItem>)}</SelectContent></Select></div>}