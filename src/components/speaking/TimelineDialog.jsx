import { Dialog,DialogContent,DialogHeader,DialogTitle } from '@/components/ui/dialog';import { formatDate } from '@/lib/speaking';
export default function TimelineDialog({open,onClose,items}){const rows=items.filter(x=>x.start_date||x.speaking_date).sort((a,b)=>(a.start_date||a.speaking_date).localeCompare(b.start_date||b.speaking_date));const dates=rows.flatMap(x=>[x.start_date,x.speaking_date]).filter(Boolean).sort(),start=dates[0]?new Date(dates[0]):new Date(),end=dates.at(-1)?new Date(dates.at(-1)):start,span=Math.max(1,end-start);return <Dialog open={open} onOpenChange={v=>!v&&onClose()}><DialogContent className="bg-[#F6F1E4] sm:max-w-3xl"><DialogHeader><DialogTitle className="font-display text-2xl">Presentation timeline</DialogTitle></DialogHeader>{!rows.length?<p className="py-12 text-center text-[#4C5B6B]">Add a creation or speaking date to see the timeline.</p>:<div className="space-y-5 py-3">{rows.map(x=>{const s=new Date(x.start_date||x.speaking_date),e=new Date(x.speaking_date||x.start_date),left=((s-start)/span)*86,width=Math.max(3,((e-s)/span)*86);return <div key={x.id}><div className="mb-2 flex justify-between gap-3 text-sm"><b>{x.title}</b><span className="text-xs text-[#4C5B6B]">{formatDate(x.start_date)} → {formatDate(x.speaking_date)}</span></div><div className="h-3 rounded-full bg-[#E4E0D4]"><div className="h-3 rounded-full bg-[#A9793B]" style={{marginLeft:`${left}%`,width:`${width}%`}}/></div></div>})}</div>}</DialogContent></Dialog>}