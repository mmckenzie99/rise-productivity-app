import { useState } from 'react';import { Input } from '@/components/ui/input';import { Label } from '@/components/ui/label';import { Button } from '@/components/ui/button';import { Loader2,MapPin } from 'lucide-react';
export default function FormLocation({form,set}){const [finding,setFinding]=useState(false);const find=async()=>{if(!form.address)return;setFinding(true);const r=await fetch(`https://nominatim.openstreetmap.org/search?format=json&limit=1&q=${encodeURIComponent(form.address)}`);const d=await r.json();if(d[0]){set('latitude',d[0].lat);set('longitude',d[0].lon)}setFinding(false)};return <><div><Label>Location address</Label><div className="flex gap-2"><Input value={form.address||''} onChange={e=>set('address',e.target.value)} placeholder="123 Main St, City"/><Button type="button" variant="outline" onClick={find} disabled={finding}>{finding?<Loader2 className="h-4 w-4 animate-spin"/>:<MapPin className="h-4 w-4"/>}</Button></div></div><div className="grid grid-cols-2 gap-4"><div><Label>Latitude</Label><Input type="number" step="any" value={form.latitude??''} onChange={e=>set('latitude',e.target.value)}/></div><div><Label>Longitude</Label><Input type="number" step="any" value={form.longitude??''} onChange={e=>set('longitude',e.target.value)}/></div></div></>}