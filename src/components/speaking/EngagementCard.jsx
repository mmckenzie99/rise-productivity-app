import { CalendarDays,MapPin,Paperclip,Copy } from 'lucide-react';
import { formatDate,formatTime,statusTone,TIMEZONES,asArray } from '@/lib/speaking';
import CardWrapper from './CardWrapper';
const ACCENT={Planning:'border-l-[#D9A404]',Confirmed:'border-l-[#1B2A4B]',Completed:'border-l-[#5A6781]'};
export default function EngagementCard({item,onClick,onDuplicate,isAdmin,hasTrip,onLocate}){return <CardWrapper onClick={()=>onClick(item)} className={`group relative cursor-pointer border-l-4 ${ACCENT[item.status]} p-5 text-left transition hover:-translate-y-1 hover:shadow-lg`}>{isAdmin&&onDuplicate&&<button type="button" onClick={e=>{e.stopPropagation();onDuplicate(item)}} className="absolute right-3 top-3 inline-flex items-center gap-1 rounded-md border border-[#D6DAE3] bg-white px-2 py-1 text-xs font-medium text-[#5A6781] opacity-0 transition group-hover:opacity-100 hover:border-[#D9A404] hover:text-[#D9A404]"><Copy className="h-3.5 w-3.5"/>Duplicate</button>}<div className="mb-3 flex items-center gap-2"><span className={`inline-flex items-center gap-1.5 rounded-full px-3 py-1 font-mono text-[11px] font-medium uppercase ${statusTone[item.status]}`}><span className="h-2 w-2 rounded-full bg-current"/>{item.status}</span></div><div className="flex flex-col items-center text-left">{item.speaker_photo&&<img src={item.speaker_photo} alt={item.speaker_name} className="mb-3 h-20 w-20 rounded-full object-cover border border-[#D6DAE3]" onError={e=>e.target.style.display='none'}/>}<p className="font-display text-lg font-semibold leading-tight w-full">{item.title}</p><p className="mt-1 text-sm text-[#5A6781] w-full">{item.speaker_name}</p></div><div className="my-4 h-px bg-[#D6DAE3]"/><div className="space-y-2 text-xs text-[#5A6781]">{item.address&&(Number.isFinite(Number(item.latitude))?<button type="button" onClick={e=>{e.stopPropagation();onLocate(item)}} className="flex gap-2 text-left transition hover:text-[#D9A404]"><MapPin className="h-4 w-4 shrink-0 text-[#D9A404]"/>{item.address}</button>:<p className="flex gap-2"><MapPin className="h-4 w-4 shrink-0 text-[#D9A404]"/>{item.address}</p>)}<p className="flex gap-2"><CalendarDays className="h-4 w-4"/>{formatDate(item.speaking_date)}{item.start_time&&` · ${formatTime(item.start_time)}`}{item.timezone&&` ${TIMEZONES.find(z=>z.value===item.timezone)?.label||item.timezone}`}</p>{item.attachments?.length>0&&<p className="flex gap-2"><Paperclip className="h-4 w-4"/>{item.attachments.length} attachment{item.attachments.length===1?'':'s'}</p>}</div><div className="mt-4 flex flex-wrap gap-2 font-mono text-[10px]"><span className="rounded-full border border-[#D6DAE3] px-2 py-1">{item.progress}</span>{asArray(item.presentation_type).map(t=><span key={t} className="rounded-full border border-[#D6DAE3] px-2 py-1">{t}</span>)}{hasTrip&&<span className="rounded-full border border-[#D9A404] bg-[#D9A404]/10 px-2 py-1 text-[#1B2A4B]">Trip</span>}</div></CardWrapper>}