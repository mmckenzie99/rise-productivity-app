import { useState } from 'react';import { base44 } from '@/api/base44Client';import { Button } from '@/components/ui/button';import { Dialog,DialogContent,DialogHeader,DialogTitle } from '@/components/ui/dialog';import { Input } from '@/components/ui/input';import { Label } from '@/components/ui/label';import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';
export default function InviteDialog({open,onClose}){const [email,setEmail]=useState(''),[role,setRole]=useState('user'),[state,setState]=useState('');const send=async(e)=>{e.preventDefault();setState('sending');try{await base44.users.inviteUser(email,role);setState('sent');setEmail('')}catch(err){setState(err.message||'Could not send invitation')}};return <Dialog open={open} onOpenChange={v=>!v&&onClose()}><DialogContent className="bg-white"><DialogHeader><DialogTitle className="font-display text-2xl">Invite a collaborator</DialogTitle><p className="text-sm text-[#5A6781]">They’ll receive an email and can sign in to this shared Speaking Log.</p></DialogHeader><form onSubmit={send} className="space-y-4"><div><Label>Email</Label><Input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@example.com" required/></div><div><Label>Role</Label><Select value={role} onValueChange={setRole}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="user">Collaborator</SelectItem><SelectItem value="admin">Administrator</SelectItem></SelectContent></Select></div>{state&&state!=='sending'&&<p className={`text-sm ${state==='sent'?'text-[#1B2A4B]':'text-[#B43A2E]'}`}>{state==='sent'?'Invitation sent.':state}</p>}<Button className="w-full bg-[#D9A404] hover:bg-[#B89003]" disabled={state==='sending'}>{state==='sending'?'Sending…':'Send invitation'}</Button></form></DialogContent></Dialog>}