import { useState } from 'react';import { base44 } from '@/api/base44Client';import { Button } from '@/components/ui/button';import { Dialog,DialogContent,DialogHeader,DialogTitle } from '@/components/ui/dialog';import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';import ResponsiveSelect from './ResponsiveSelect';
export default function InviteDialog({open,onClose}){const [email,setEmail]=useState(''),[role,setRole]=useState('user'),[state,setState]=useState('');const send=async(e)=>{e.preventDefault();setState('sending');try{await base44.users.inviteUser(email,role);setState('sent');setEmail('')}catch(err){setState(err.message||'Could not send invitation')}};return <Dialog open={open} onOpenChange={v=>!v&&onClose()}><DialogContent className="flex inset-0 max-h-none max-w-none flex-col overflow-hidden gap-0 p-0 bg-white translate-x-0 translate-y-0 rounded-none sm:top-[50%] sm:left-[50%] sm:right-auto sm:bottom-auto sm:max-h-[90dvh] sm:max-w-md sm:translate-x-[-50%] sm:translate-y-[-50%] sm:rounded-lg"><DialogHeader className="shrink-0 border-b border-[#D6DAE3] px-6 pt-[calc(1.5rem+env(safe-area-inset-top))] pb-3"><DialogTitle className="font-display text-2xl">Invite a collaborator</DialogTitle><p className="text-sm text-[#5A6781]">They’ll receive an email and can sign in to RISE.</p></DialogHeader><form onSubmit={send} className="flex-1 min-h-0 overflow-y-auto overscroll-contain px-6 py-4 pb-[calc(1.5rem+env(safe-area-inset-bottom))] space-y-4"><div><Label>Email</Label><Input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@example.com" required/></div><div><Label>Role</Label><ResponsiveSelect value={role} onValueChange={setRole} options={[{value:'user',label:'Collaborator'},{value:'admin',label:'Administrator'}]} /></div>{state&&state!=='sending'&&<p className={`text-sm ${state==='sent'?'text-[#1B2A4B]':'text-[#B43A2E]'}`}>{state==='sent'?'Invitation sent.':state}</p>}<Button className="w-full bg-[#D9A404] hover:bg-[#B89003]" disabled={state==='sending'}>{state==='sending'?'Sending…':'Send invitation'}</Button></form></DialogContent></Dialog>}