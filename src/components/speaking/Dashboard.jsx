import { CalendarDays,Clock3,Tags } from 'lucide-react';
import { daysUntil } from '@/lib/speaking';
export default function Dashboard({items}){
 const today=new Date().toISOString().slice(0,10), upcoming=items.filter(x=>x.speaking_date>=today&&x.status!=='Completed');
 const urgent=upcoming.filter(x=>x.progress!=='Ready to Deploy'&&daysUntil(x.speaking_date)<=14);
 const types=items.reduce((a,x)=>({...a,[x.presentation_type]:(a[x.presentation_type]||0)+1}),{}),top=Object.entries(types).sort((a,b)=>b[1]-a[1])[0];
 const cards=[{icon:CalendarDays,label:'Upcoming engagements',value:upcoming.length,note:'Scheduled ahead'},{icon:Clock3,label:'Upcoming deadlines',value:urgent.length,note:'Within 14 days'},{icon:Tags,label:'Top presentation type',value:top?.[1]||0,note:top?.[0]||'No type set'}];
 return <section><h2 className="mb-3 font-display text-lg font-semibold">At a glance</h2><div className="grid gap-3 md:grid-cols-3">{cards.map(({icon:Icon,label,value,note},i)=><article key={label} className={`rounded-lg border border-[#C9BE9C] border-l-4 ${i===1?'border-l-[#9C4B3A]':i===2?'border-l-[#A9793B]':'border-l-[#3F6E63]'} bg-[#F6F1E4] p-5 shadow-sm transition hover:-translate-y-0.5`}><div className="flex items-center gap-2 font-mono text-[10px] uppercase tracking-wider text-[#4C5B6B]"><Icon className="h-4 w-4"/>{label}</div><p className="mt-3 font-display text-4xl font-bold">{value}</p><p className="mt-1 text-xs text-[#4C5B6B]">{note}</p></article>)}</div></section>}