import { MapContainer,Marker,Popup,TileLayer,useMap } from 'react-leaflet';
import 'leaflet/dist/leaflet.css';
import L from 'leaflet';
import { asArray } from '@/lib/speaking';
const icon=L.divIcon({html:'<div style="width:18px;height:18px;background:#D9A404;border:2px solid #FFFFFF;border-radius:50% 50% 50% 0;transform:rotate(-45deg);box-shadow:0 2px 4px #1B2A4B66"></div>',className:'',iconSize:[20,20],iconAnchor:[10,20]});
function Fit({points}){const map=useMap();if(points.length) setTimeout(()=>map.setView(points[points.length-1],18,{animate:true}),0);return null}
export default function EngagementMap({items,onView}){
  const located=items.filter(x=>Number.isFinite(Number(x.latitude))&&Number.isFinite(Number(x.longitude)));
  const groups=Object.values(located.reduce((acc,x)=>{const key=`${x.latitude},${x.longitude}`;if(!acc[key])acc[key]={lat:x.latitude,lng:x.longitude,address:x.address||'',items:[]};acc[key].items.push(x);return acc},{}));
  return <section className="overflow-hidden rounded-lg border border-[#D6DAE3] bg-white shadow-sm"><MapContainer center={[39.8,-98.6]} zoom={4} scrollWheelZoom={false} className="h-[320px] w-full"><TileLayer attribution="Tiles © Esri" url="https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}"/>{groups.map(g=><Marker key={`${g.lat},${g.lng}`} position={[g.lat,g.lng]} icon={icon} eventHandlers={{click:()=>onView(g.items[0]),mouseover:e=>e.target.openPopup(),mouseout:e=>e.target.closePopup()}}><Popup><div className="text-sm"><div className="font-semibold">{g.address}</div>{g.items.map((x,i)=><div key={i} className="mt-1"><span className="text-xs text-gray-700">{x.title}</span>{asArray(x.presentation_type).length>0&&<span className="text-xs text-gray-500"> — {asArray(x.presentation_type).join(', ')}</span>}</div>)}</div></Popup></Marker>)}<Fit points={located.map(x=>[x.latitude,x.longitude])}/></MapContainer><div className="flex justify-between border-t border-[#D6DAE3] px-4 py-2 font-mono text-[11px] text-[#5A6781]"><span>{items.length} engagement{items.length===1?'':'s'}</span><span>{located.length} mapped</span></div></section>}