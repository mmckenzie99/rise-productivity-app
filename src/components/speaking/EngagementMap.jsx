import { MapContainer,Marker,Popup,TileLayer,useMap } from 'react-leaflet';
import 'leaflet/dist/leaflet.css';
import L from 'leaflet';
const icon=L.divIcon({html:'<div style="width:18px;height:18px;background:#D9A404;border:2px solid #FFFFFF;border-radius:50% 50% 50% 0;transform:rotate(-45deg);box-shadow:0 2px 4px #1B2A4B66"></div>',className:'',iconSize:[20,20],iconAnchor:[10,20]});
function Fit({points}){const map=useMap();if(points.length) setTimeout(()=>map.setView(points[points.length-1],18,{animate:true}),0);return null}
export default function EngagementMap({items,onView}){const located=items.filter(x=>Number.isFinite(Number(x.latitude))&&Number.isFinite(Number(x.longitude)));return <section className="overflow-hidden rounded-lg border border-[#D6DAE3] bg-white shadow-sm"><MapContainer center={[39.8,-98.6]} zoom={4} scrollWheelZoom={false} className="h-[320px] w-full"><TileLayer attribution="Tiles © Esri" url="https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}"/>{located.map(x=><Marker key={x.id} position={[x.latitude,x.longitude]} icon={icon} eventHandlers={{click:()=>onView(x),mouseover:e=>e.target.openPopup(),mouseout:e=>e.target.closePopup()}}><Popup>{x.address}{x.presentation_type&&<><br/><span className="text-xs text-gray-500">{x.presentation_type}</span></>}</Popup></Marker>)}<Fit points={located.map(x=>[x.latitude,x.longitude])}/></MapContainer><div className="flex justify-between border-t border-[#D6DAE3] px-4 py-2 font-mono text-[11px] text-[#5A6781]"><span>{items.length} engagement{items.length===1?'':'s'}</span><span>{located.length} mapped</span></div></section>}