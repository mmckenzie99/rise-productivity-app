import { MapContainer,Marker,Popup,TileLayer,useMap } from 'react-leaflet';
import 'leaflet/dist/leaflet.css';
import L from 'leaflet';
const icon=L.divIcon({html:'<div style="width:18px;height:18px;background:#A9793B;border:2px solid #F6F1E4;border-radius:50% 50% 50% 0;transform:rotate(-45deg);box-shadow:0 2px 4px #1F2D3B66"></div>',className:'',iconSize:[20,20],iconAnchor:[10,20]});
function Fit({points}){const map=useMap();if(points.length) setTimeout(()=>map.setView(points[points.length-1],18,{animate:true}),0);return null}
export default function EngagementMap({items,onView}){const located=items.filter(x=>Number.isFinite(Number(x.latitude))&&Number.isFinite(Number(x.longitude)));return <section className="overflow-hidden rounded-lg border border-[#C9BE9C] bg-[#F6F1E4] shadow-sm"><MapContainer center={[39.8,-98.6]} zoom={4} scrollWheelZoom={false} className="h-[320px] w-full"><TileLayer attribution="Tiles © Esri" url="https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}"/>{located.map(x=><Marker key={x.id} position={[x.latitude,x.longitude]} icon={icon} eventHandlers={{click:()=>onView(x)}}><Popup><b>{x.title}</b><br/>{x.address}</Popup></Marker>)}<Fit points={located.map(x=>[x.latitude,x.longitude])}/></MapContainer><div className="flex justify-between border-t border-[#C9BE9C] px-4 py-2 font-mono text-[11px] text-[#4C5B6B]"><span>{items.length} engagement{items.length===1?'':'s'}</span><span>{located.length} mapped</span></div></section>}