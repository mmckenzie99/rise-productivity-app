{
  "name": "ChatMessage",
  "type": "object",
  "properties": {
    "room_id": {
      "type": "string",
      "description": "ID of the ChatRoom this message belongs to"
    },
    "body": {
      "type": "string",
      "description": "Message text"
    },
    "attachment": {
      "type": "object",
      "description": "Optional file shared in the message",
      "properties": {
        "name": {
          "type": "string",
          "description": "Original file name"
        },
        "url": {
          "type": "string",
          "description": "Public URL of the uploaded file"
        }
      }
    },
    "author_id": {
      "type": "string",
      "description": "User id of the sender. Set to 'system' for system-authored audit messages."
    },
    "author_name": {
      "type": "string",
      "description": "Cached display name of the sender"
    },
    "is_system": {
      "type": "boolean",
      "default": false,
      "description": "True for system-authored audit messages (e.g. 'Marshall added Rosemary'). These are created server-side via the service role only; the create-RLS blocks regular clients from setting this to true."
    },
    "participant_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Snapshot of the room's participant ids at send time, used to gate read access"
    }
  },
  "required": [
    "room_id",
    "body"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.participant_ids": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "$or": [
        {
          "$and": [
            {
              "created_by_id": "{{user.id}}"
            },
            {
              "data.is_system": {
                "$ne": true
              }
            }
          ]
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}