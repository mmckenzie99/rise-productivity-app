{
  "name": "PlanComment",
  "type": "object",
  "properties": {
    "calendar_event_id": {
      "type": "string",
      "description": "ID of the plan (CalendarEvent) this comment belongs to"
    },
    "body": {
      "type": "string",
      "description": "Comment text"
    },
    "author_name": {
      "type": "string",
      "description": "Cached display name of the comment author"
    },
    "notify_admin_id": {
      "type": "string",
      "description": "ID of the administrator notified about this comment"
    },
    "notify_admin_name": {
      "type": "string",
      "description": "Cached display name of the notified admin"
    }
  },
  "required": [
    "calendar_event_id",
    "body"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}